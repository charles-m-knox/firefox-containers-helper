export const getContainer = browser.contextualIdentities.get;
export const updateContainer = browser.contextualIdentities.update;
export const createContainer = browser.contextualIdentities.create;
export const removeContainer = browser.contextualIdentities.remove;
export const queryContainers = browser.contextualIdentities.query;
